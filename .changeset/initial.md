---
'@vskstudio/takt-svelte': minor
---

Initial release: Svelte wrapper for `@vskstudio/takt-core` with three entry points — idiomatic `<Takt />` component + `useTakt()`, `<takt-analytics>` web component (`/element`), and the functional `taktEvent` action (`/actions`).
